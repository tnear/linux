QUOTE

'Single' quotes are known as hard quotes.
"Double" quotes are known as soft quotes.

# Single quotes preserve the literal value of each character:
$ var=hello
$ echo '$var'
$var

# Double quotes preserve the special meaning of characters ($, `, ", \):
$ var=hello
$ echo "$var"
hello

---