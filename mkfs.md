# mkfs

`mkfs` - build a Linux filesystem

See also: [`mke2fs`](mke2fs.md)

## Introduction

`mkfs` is a generic for creating filesystems. Other commands, such as `mkfs.ext4` and `mke2fs` are specialized to create a specific type of filesystem (ex: ext2/ext3/ext4).
