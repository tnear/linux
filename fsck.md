# fsck

`fsck` - check and repair a Linux filesystem

File system consistency check (`fsck`) fixes filesystems inconsistencies.

## When to use `fsck`
- After system crash or power failure
- If system reports filesystem errors
- As part of scheduled maintenance
