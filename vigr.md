# vigr

`vigr` - edit the password, group, shadow-password or shadow-group file

See also: [`vipw`](vipw.md)

`vipw` is an administrative command for safely editing the system group database, `/etc/group`. It locks the file to prevent concurrent access.

## Basic usage

```bash
$ sudo vigr
```
